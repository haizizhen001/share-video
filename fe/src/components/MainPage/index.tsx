import MainLayout from '../Layout/MainLayout';
import ListVideos from './ListVideos';

const MainPage = () => {
  return (
    <MainLayout>
      <ListVideos />
    </MainLayout>
  );
};

export default MainPage;
